import 'package:flutter/material.dart';

class ComponentScaffold extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: const Text('Skin Saga'),
      // ),
      body: Center(
        child: Text('Splash'),
      ),
    );
  }
}